{
  "name": "@youpd/notion-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(notion-worker) no lint configured\"",
    "test": "vitest run",
    "e2e:tracking": "tsx scripts/e2e-tracking-due.ts"
  },
  "dependencies": {
    "@notionhq/client": "5.21.0",
    "@notionhq/workers": "0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
